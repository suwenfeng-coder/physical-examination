CREATE DATABASE IF NOT EXISTS physical_examination
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

-- 表结构由 Spring Data JPA 在首次启动时自动创建和升级。
-- application.yml 中 spring.jpa.hibernate.ddl-auto=update。
